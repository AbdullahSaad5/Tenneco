# ============================================================================
# Docker Compose Environment Variables
# ============================================================================
# This file contains ALL environment variables needed for docker-compose.yml
# Copy this file to .env and fill in your actual values
#
# Usage:
#   1. cp docker.env.example .env
#   2. Edit .env with your actual values
#   3. Run: docker-compose up -d
#
# IMPORTANT: The .env file is gitignored - never commit it!
# ============================================================================

# ----------------------------------------------------------------------------
# MongoDB Configuration
# ----------------------------------------------------------------------------
MONGO_PASSWORD=changeme123

# MongoDB connection string (used by Admin service)
# For Docker: Use the service name 'mongodb' as hostname
# For production: Use MongoDB Atlas connection string
DATABASE_URI=mongodb://admin:changeme123@mongodb:27017/tenneco?authSource=admin

# ----------------------------------------------------------------------------
# Payload CMS Configuration (Admin Service)
# ----------------------------------------------------------------------------

# CRITICAL: Change this to a secure random string in production!
# Generate with: openssl rand -base64 32
PAYLOAD_SECRET=your-secret-here-change-in-production

# Public server URL for Payload CMS
# This is the URL where the admin panel is accessible
# For Docker: http://localhost:3001 (mapped port)
# For production: https://your-admin-domain.com
PAYLOAD_PUBLIC_SERVER_URL=http://localhost:3001

# ----------------------------------------------------------------------------
# AWS S3 Configuration (Media Storage)
# ----------------------------------------------------------------------------
# Required for storing and serving media files (images, PDFs, 3D models)

S3_ACCESS_KEY_ID=your-access-key-id-here
S3_SECRET_ACCESS_KEY=your-secret-access-key-here
S3_REGION=eu-central-1
S3_BUCKET=tenneco-test

# Optional: CloudFront CDN URL for faster media delivery
# S3_CLOUDFRONT_URL=https://your-cloudfront-domain.cloudfront.net

# ----------------------------------------------------------------------------
# Client (Frontend) Configuration
# ----------------------------------------------------------------------------

# API URL for client to connect to admin service
# For Docker: Use internal service name 'admin' (http://admin:3000/api)
# For local development: http://localhost:3001/api
# For production: https://your-api-domain.com/api
NEXT_PUBLIC_API_URL=http://admin:3000/api

# Enable/Disable CMS integration
NEXT_PUBLIC_ENABLE_CMS=true

# Fallback timeout for API requests (milliseconds)
NEXT_PUBLIC_FALLBACK_TIMEOUT=5000

# Cache duration for CMS content (milliseconds)
NEXT_PUBLIC_CACHE_DURATION=300000

# ============================================================================
# Environment-Specific Examples
# ============================================================================

# --- LOCAL DEVELOPMENT (without Docker) ---
# NEXT_PUBLIC_API_URL=http://localhost:3001/api
# DATABASE_URI=mongodb://localhost:27017/tenneco

# --- DOCKER DEVELOPMENT ---
# NEXT_PUBLIC_API_URL=http://admin:3000/api
# DATABASE_URI=mongodb://admin:changeme123@mongodb:27017/tenneco?authSource=admin

# --- PRODUCTION ---
# NEXT_PUBLIC_API_URL=https://api.yourproductiondomain.com/api
# DATABASE_URI=mongodb+srv://username:password@cluster.mongodb.net/tenneco
# PAYLOAD_SECRET=$(openssl rand -base64 32)
# PAYLOAD_PUBLIC_SERVER_URL=https://admin.yourproductiondomain.com

# ============================================================================
