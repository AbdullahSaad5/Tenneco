# ============================================================================
# Tenneco 3D Viewer - Environment Variables
# ============================================================================

# ----------------------------------------------------------------------------
# API Configuration
# ----------------------------------------------------------------------------

# Payload CMS API URL
# Points to your deployed Payload CMS backend
# For Docker: http://admin:3000/api (uses internal Docker network)
# For local development: http://localhost:3001/api
# For production: https://tenneco-admin.vercel.app/api
NEXT_PUBLIC_API_URL=http://localhost:3001/api

# ----------------------------------------------------------------------------
# CMS Feature Flags
# ----------------------------------------------------------------------------

# Enable/Disable Dynamic CMS Content
# Set to "true" to fetch content from CMS, "false" to use fallbacks only
# Default: true (CMS enabled)
NEXT_PUBLIC_ENABLE_CMS=true

# ----------------------------------------------------------------------------
# Fallback Configuration
# ----------------------------------------------------------------------------

# Timeout for CMS API requests (milliseconds)
# If API doesn't respond within this time, fallback content is used
# Default: 5000ms (5 seconds)
NEXT_PUBLIC_FALLBACK_TIMEOUT=5000

# Cache duration for CMS content (milliseconds)
# How long to cache content before refetching
# Default: 300000ms (5 minutes)
NEXT_PUBLIC_CACHE_DURATION=300000

# ----------------------------------------------------------------------------
# Notes
# ----------------------------------------------------------------------------

# 1. Copy this file to .env.local for local development
# 2. For Docker, create a .env file in the client folder (see docker.env.example)
# 3. The .env.local and .env files are gitignored and won't be committed
# 4. For production, set these variables in your deployment platform (Vercel, etc.)
# 5. All variables starting with NEXT_PUBLIC_ are exposed to the browser
# 6. Never commit sensitive data or API keys to this file
